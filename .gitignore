node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store

# Local Netlify folder
.netlify

# Binary packages
*.pkg
*.zip

# Capacitor native build output — the native projects live on feat/capacitor-native,
# not on main; these are regenerated by `npx cap add` and shouldn't be committed here.
/android/
/ios/

# Binary docs & design assets (kept in Drive / locally, not tracked in the repo)
*.pptx
*.pdf
*.docx
/Screenshots/
docs/**/*.png

# Claude Code local runtime state
.claude/
